<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- Merged into the app manifest by the Android manifest merger; survives
       `expo prebuild` because this library lives outside android/.
       See docs/feasibility-and-test-protocol.md for why the wake chain has
       to be a plain BroadcastReceiver + Service (no JS at fire time). -->
  <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:maxSdkVersion="32"/>
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
  <uses-permission android:name="android.permission.WAKE_LOCK"/>
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
  <application>
    <receiver
      android:name="com.richinevan.quietalarm.alarm.AlarmReceiver"
      android:exported="false"/>
    <service
      android:name="com.richinevan.quietalarm.alarm.AlarmAudioService"
      android:exported="false"
      android:foregroundServiceType="mediaPlayback"/>
  </application>
</manifest>
